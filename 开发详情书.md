# 万幕生（Manvo AI Studio）系统开发现状全景与后续开发详情书

> 撰写日期：2026-09-11
> 面向读者：产品负责人、开发团队、投资与合作方
> 配套文档：`docs/PROGRESS.md`（进度与交接）、`docs/api-reference.md`（接口参考）、`docs/plans/`（历次执行计划与日志）、`docs/superpowers/specs/`（统一模型配置设计）

---

# 第一部分：现有系统全景分析

## 1.1 产品定位与核心价值

万幕生是一个面向短剧/漫剧创作者的 **AI 影视创作全流程平台**。它把「一个想法 → 一部可发布的竖屏短剧」所需的全部环节收进一条生产流水线：立项分析、剧本会诊、蓝图与分集、角色/场景/道具资产管理、分镜拆分、首帧图与视频生成、配音与 BGM、后期合成。产品形态是深色主题的 Web SaaS，以「工作区 + 积分」为运营基座。

核心价值主张有三条：

1. **流程闭环**：市场上多数工具只解决单点（如仅文生图、仅剪辑），万幕生把创意到成片的全部环节串成一条有状态的生产线，每一环的产物自动成为下一环的输入——剧本分析产出视觉风格，资产提取产出角色卡，分镜引用资产保证一致性，视频挂首帧图防穿帮。
2. **一致性问题**：短剧量产最痛的「角色脸跨镜头漂移」被作为一等公民处理：角色/妆造/道具/场景四类资产各自带参考图与锁定机制，分镜与视频生成时通过引用与调度图约束画面。
3. **模型无关**：统一模型配置体系（CustomModel）让平台可以在不同时期接不同供应商——线上 API（Qwen、DeepSeek、wan2.7-image、火山 Seedance、可灵等）、本地 ComfyUI（Z-Image Turbo 文生图、MiniMax-H3 图生视频）——而业务代码不感知供应商切换。

## 1.2 技术架构基础

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Next.js 14 App Router + React 18 + Tailwind v3 + shadcn/ui + framer-motion | 仅深色主题，zinc + 品牌橙 |
| 画布 | @xyflow/react v12 | 自研七类节点 + 沉浸式画布操作页（导演台 iframe 集成） |
| 状态 | Zustand + React Hook Form + Zod | 表单校验统一走 Zod schema |
| 数据库 | PostgreSQL + Prisma 6 | 20+ 模型：Script/Episode/Character/Costume/Scene/Prop/Storyboard/Consultation/WritingProject/MediaFile/CustomModel/Credential/SystemSetting/EcomProject 等 |
| 认证 | NextAuth v4（Credentials + JWT） | 会话携带积分（tapies）与会员等级 |
| AI 抽象层 | `src/services/ai/`（types/mock/live） | 15 个能力方法；mock/live 由 SystemSetting.ai_mode 运行时切换 |
| 模型体系 | CustomModel（providerType=api|comfyui）+ 18 个接入模板 + transformBody 钩子 + Credential | 任意 REST 形态与本地 ComfyUI 均可接入 |
| 测试 | Playwright E2E（40 用例）+ HTTP 冒烟（17 项） | 全绿为提交基线 |

**运行时模式开关**：SystemSetting.ai_mode = mock | live。mock 下全部能力由内置 Mock 实现（无 Key 可跑通全流程）；live 下文本走 Qwen3.7-plus（主力）/DeepSeek（辅助），图片/视频按「线上 CustomModel → 本地 ComfyUI → 明确报错」顺序分发。这一机制是商业化计费与成本控制的关键基础设施。

## 1.3 模块逐个分析

### 1.3.1 工作台（Workbench）

多模态快捷生成入口：文本/图片/视频/音频四类媒体切换，提示词输入、参考素材（@ 引用）、模型与参数（画幅/清晰度/时长）、积分扣减、作品画廊（横向滚动、hover 播放）。工作台是「轻创作」场景，与影视工厂的「重流程」形成互补。

- 现状亮点：生成全链路可用；画廊沉淀作品；模型选择器已接入动态模型数据（useAiModels hook），mock/live 自动切换。
- 主要短板：生成历史无持久检索（按项目名搜索有限）；缺乏「从画廊一键送入影视工厂/画布」的资产流转；提示词无模板库与收藏。

### 1.3.2 画布（Canvas）

React Flow 节点编排器：文本/图片/视频/音频/合流等七类节点，连线表达依赖，⌘S 保存，撤销/重做，右键菜单，沉浸式画布操作页（底部居中工具条、画布/资产双抽屉、连线磁性跟随、导演台 iframe——由外部工作流批量投递节点产物）。

- 现状亮点：节点系统稳定，画布操作页交互（放大镜预览、卡面操作条）已对齐原型。
- 主要短板：节点产物与影视工厂资产不互通；缺「画布 → 导出工程包」；多人协作未开始。

### 1.3.3 影视工厂（Film Factory，核心重流程）

现状最完整的生产线，包含：

1. **建档（INTAKE）**：粘贴剧本/文案 → AI 智能立项（通读全本推荐集数/时长/三幕结构/题材/视觉风格/服化道）→ 审阅弹窗微调 → 创建分集。已改为列表页弹窗承载。
2. **剧本会诊**：AI 按结构/人物/节奏/台词/逻辑五类输出诊断（严重度、是否必改），支持勾选生成台词优化、按诊断项与 AI 对话讨论改法。
3. **蓝图与分集**：生成项目蓝图（世界观/结构/钩子），逐集生成正文（编剧 Agent）。
4. **资产管理**：提取角色/场景/道具描述词（大模型拆分，携带图片模型名做提示词适配）→ 资产参考图生成（角色设定卡/道具设定卡/场景设定卡三种卡面构图模板）→ 妆造库（角色默认造型 + 手动新建造型，角色为父级）→ 资产卡操作条（查看大图/下载/上传替换/编辑/锁定/合并去重/删除/补缺漏/提示词模板——四类模板独立）→ 空间资产（多角度 5 宫格 + 侧别锁定卡 + 光影设计卡）。
5. **分镜系统**：AI 拆分镜（镜组分段 segmentTitle/segmentNote）、出片前检查（必须解决/建议补齐/相邻衔接三类）、镜组分段卡（段头状态 chips + 补全首帧图 + 段资产弹窗 + 编辑整段引用弹窗）、逐镜出图/出视频、批量生成视频（占位框按画幅动态）、分镜提示词时间轴。
6. **视频与后期**：逐镜出视频、批量出片队列（VideoBatchDialog + VideoProgressBar）、BGM/配音生成、后期合成导出面板。

- 现状亮点：全流程闭环已通；资产一致性体系完整；镜组/出片前检查/引用体系落地。
- 主要短板：视频生成依赖外部模型（线上 API 或 ComfyUI），当前无内置云端视频供应商凭证时 live 体验断链；镜组编辑（跨镜拖拽重组）缺失；批量任务的服务端持久化队列未建（刷新丢失进度展示）。

### 1.3.4 剧本创作（Script Writing）

独立于影视工厂的「编剧工坊」：列表（场记板卡片、置顶/编辑/删除菜单、统计卡）→ 弹窗新建（题材配方 + 一句话灵感 + 集数/时长预设）→ 编剧工作区（项目蓝图生成、分集正文编辑、版本历史恢复、角色档案、导入影视工厂）。列表页样式已与影视工厂完全对齐。

- 现状亮点：草稿恢复（浏览器前进后退不丢正文）；版本历史；一键导入影视工厂打通两条创作线。
- 主要短板：无多人大纲协作；蓝图→分集的生成策略较单一（缺少「按会诊建议重写」「按爆款结构重排」等结构化重写动作）。

### 1.3.5 电商设计室（E-commerce Studio）

商品套图（平台/地区/语言/画幅、卖点 AI 帮写、结构配置、风格分析、逐项出图）与 A+ 详情页（拖拽排序区块）。复用统一 AI 抽象层。

- 现状亮点：与影视工厂共享模型体系与积分；套图结构可配置。
- 主要短板：模板较少；缺白底图抠图/多语言文案本地化等电商特化能力。

### 1.3.6 AI 设置与插件体系（/ai-settings）

统一模型配置的唯一入口：MOCK 开关、凭据管理、自定义模型（18 个模板：chat-openai/anthropic/gemini、txt2img、img2img 系列、vid 系列、music/sfx、whisper-asr、volc-seedance）、模型连通性测试、providerType=api|comfyui 双通道、transformBody 钩子（JS 预处理请求体，覆盖任意 API 形态）、字幕 ASR 能力。

- 现状亮点：架构统一且可扩展；mock/live 干净切换；字幕能力已进入模型体系。
- 主要短板：模型分组排序未做；缺工作流 JSON 可视化上传（ComfyUI 工作流目前靠代码内置模板）；模板文档对非技术用户仍偏难。

### 1.3.7 基础设施

媒体库（MediaFile 上传/访问）、积分体系（会话 tapies，生成扣费，演示账号重置脚本）、内嵌 PostgreSQL、Playwright/冒烟双测试线、Docker 配置、GitHub 仓库（wanmu-ai-studio）。

## 1.4 优势与短板总结（SWOT）

- **S（优势）**：全流程闭环且状态连贯；资产一致性问题被系统性处理；模型无关架构（mock/live 无缝切换）；深色专业 UI 已对齐产品原型；测试基线全绿。
- **W（短板）**：视频/音频的真实供应商通道待补齐凭证；批量任务无服务端队列（长任务依赖浏览器页面存活）；多人协作与权限体系薄弱；模板/素材生态未建立。
- **O（机会）**：短剧市场高速增长且工具侧竞争格局未定；ComfyUI 本地化生成成本极低（电费价）；token-plan 等聚合网关让单一凭据覆盖多模型；字幕/多语言出海是天然延伸。
- **T（威胁）**：大模型 API 价格与政策波动；内容审核合规（生成内容需标识）；同类产品（剪映 AI、即梦、Runway 等）自带流量入口。

---

# 第二部分：后续功能开发路线图（按优先级详细展开）

> 估时单位：人日（1 名全栈工程师）。每项含：背景动机、方案要点、数据模型、关键接口、UI 要点、验收标准。

## P0-1 云端视频供应商接入（预计 5 人日）

**背景**：视频是生产链路的终点也是商业价值最高的能力。当前 live 视频通道依赖本地 ComfyUI（用户机器离线即断）。
**方案**：按统一模型体系接入 2-3 家云端视频 API：火山 Seedance（模板已内置，transformBody 已适配 content 数组）、可灵 Kling（异步任务 + 轮询）、Runway Gen-3（可选）。在 /ai-settings 用「视频」模板 + 凭据一键接入，constraints 里声明支持的比例/时长/分辨率。
**数据模型**：无新增；CustomModel(kind=video, lifecycle=async, poll={...})。
**关键接口**：复用 `POST /api/storyboards/[id]/generate`（kind=video）与 `invokeCustomModel`。
**验收**：live 模式下单镜出视频成功率 ≥95%；批量生成视频支持断点续跑（已出视频跳过）。

## P0-2 服务端任务队列与任务中心（预计 8 人日）

**背景**：长任务（拆分镜、批量出图、批量出视频、整剧生成）目前依赖浏览器发起并等待，刷新/关页即失。
**方案**：新增 `Job` 模型（id、workspaceId、type、payload、status: queued|running|done|failed|canceled、progress、steps、result、error、createdAt/finishedAt）。Route 落库排队，worker（setInterval 起的进程内 worker 或独立脚本）消费；SSE（`/api/jobs/[id]/stream`）推送进度；页面任务中心（右上角铃铛下拉）显示进行中/历史，支持取消与重试。影视工厂现有的 TaskQueueDialog 升级为读 Job 表。
**验收**：批量出视频刷新页面后任务继续；任务中心可看到实时步数（如「出视频 3/7」）。

## P0-3 视频后期合成与导出（预计 6 人日）

**背景**：逐镜视频生成后缺少「合成成片」一步，用户仍需外部剪辑工具。
**方案**：引入 ffmpeg.wasm（浏览器端）或服务端 ffmpeg：按分镜顺序拼接视频、叠加字幕（SRT 已由字幕 ASR 能力产出）、混入 BGM/配音音轨、转码目标分辨率与码率；导出 MP4 落 MediaFile。后期合成面板（已存在）接真实逻辑。
**数据模型**：Export 任务走 Job 体系；产物落 MediaFile。
**验收**：一集 7 镜 + BGM + 字幕可一键导出 1080x1920 MP4。

## P0-4 字幕生成与烧录（预计 4 人日）

**背景**：字幕 ASR 能力已进入模型体系（Whisper 模板 + Mock），但尚无 UI 消费。
**方案**：分镜详情新增「生成字幕」入口（对台词/音频 ASR 生成 SRT）；后期合成时可选烧录；字幕样式（字号/描边/位置）可配置。台词驱动的剧集也可直接由分镜 dialogue 合成字幕（无 ASR 成本）。
**验收**：一集视频导出后字幕同步率 ≥95%。

## P1-1 分镜编辑器升级：时间线与拖拽（预计 8 人日）

**背景**：当前分镜以卡片网格展示，重组镜组、调整顺序需逐镜操作。
**方案**：镜组段内支持拖拽排序（dnd-kit）；跨段拖拽改 segmentTitle；时间线视图（横轴 = 累计时长，块高 = duration）；块上直接改时长；与「分镜提示词时间轴」联动高亮。
**数据模型**：Storyboard 增加 sortOrder（或复用 number 重排）。
**验收**：拖拽后 number/时长自动重算并保存；时间轴与卡片双向同步。

## P1-2 角色一致性工作流：LoRA/人脸参考训练闭环（预计 10 人日）

**背景**：参考图 + 调度图能缓解漂移，但大量镜头下仍需角色专属模型。
**方案**：接入 ComfyUI 训练插件（如 flux-trainer）或云端训练 API；角色详情页新增「训练专属模型」：上传 10-20 张角色图 → 训练 LoRA → 训练产物登记为 CustomModel(kind=image, constraints.character_lora)；出图时自动注入 LoRA 节点。训练任务走 Job 队列。
**验收**：同一角色跨 10 个镜头的 facial consistency 人工评分显著提升。

## P1-3 资产库与素材市场（预计 6 人日）

**背景**：资产目前归属于单剧本，跨剧本复用靠重新生成。
**方案**：新增「资产库」页：全工作区角色/场景/道具/造型聚合浏览、收藏、跨剧本导入（复制或引用）；进一步可做「资产市场」（用户上传设定卡出售，平台抽成）。
**数据模型**：Asset 增加 libraryScope（script|workspace|public）或新建 LibraryAsset 表 + 引用计数。
**验收**：新剧本可一键引用库中角色并保留其参考图与 LoRA。

## P1-4 多人协作与权限（预计 10 人日）

**背景**：工作区（Workspace）与成员关系已存在，但无角色权限与实时协同。
**方案**：角色分 owner/editor/viewer；编辑锁（剧本/分镜级，Redis 或 DB 行锁 + 心跳）；操作审计（AuditLog）；@ 提醒（站内信）。实时协同（Yjs CRDT）先覆盖剧本正文编辑，画布协同后置。
**验收**：两人同时编辑不同分镜互不覆盖；viewer 只读可见。

## P1-5 移动端适配与 H5 预览（预计 5 人日）

**背景**：制片人在外需要用手机审片、批注、盯进度。
**方案**：分镜/审片页做移动优先布局（卡片纵向流、手势滑动切镜）；审批动作（通过/打回）落 Comment 模型；微信 H5 分享链接（带签名与有效期）。
**验收**：手机浏览器完成「审一集成片并打回一个镜头」全流程。

## P2-1 数据看板与成本分析（预计 4 人日）

**方案**：工作区看板：生成次数/积分消耗/模型分布/成功率趋势图；单剧本成本核算（按 Job 聚合模型调用与积分）；异常调用告警。为定价与毛利分析提供数据。

## P2-2 内容安全与合规（预计 4 人日）

**方案**：生成前后双端审核（文本敏感词 + 图片审核 API）；生成图片/视频隐式水印（SVG 元数据或帧水印）；AIGC 内容标识（按《生成式人工智能服务管理暂行办法》要求在导出物中打标）；安全改写开关（分镜图弹窗已有，扩展到全部生成入口）。

## P2-3 API 开放平台（预计 8 人日）

**方案**：把核心能力（文本/图片/视频/字幕）以 REST API + API Key 形式对外开放，复用 CustomModel 体系做上游、积分做计费；开发者控制台（Key 管理、用量、账单）。这是从「工具」走向「平台」的关键一步。

## P2-4 模板与预设生态（预计 6 人日）

**方案**：提示词模板库（题材 × 风格 × 镜组节奏的官方预设 + 用户自定义 + 分享）；一键「爆款结构重排」（按钩子-冲突-反转模板重排分集）；工作流市场（ComfyUI 工作流 JSON 上传/分享，弥补当前内置模板的限制——设计文档第五节已列为改进项）。

## P2-5 智能剧务辅助（预计 6 人日）

**方案**：基于会诊/校验数据的「自动修复」一键执行（补首帧、重拆镜、挂场景已可单点执行，串成修复计划）；排期表（按集生成产能/成本预估）；成片自动剪辑风格预设（卡点/快切/慢叙事）。

## P3 远期方向

1. **数字人与口播**：TTS（qwen-audio-tts 已在网关可用）+ 口型视频（Wan/Scail 工作流已在用户素材库）→ 有声漫剧/口播剧一条龙。
2. **多语言出海**：剧本多语言改写 + 字幕翻译 + 配音克隆，面向东南亚/欧美短剧市场。
3. **IP 资产管理**：系列剧共享世界观（角色/场景/设定集跨季复用）。
4. **插件 SDK**：开放第三方节点/模板接入规范，形成开发者生态。

---

# 第三部分：在当前环境下的盈利策略

## 3.1 目标客群与付费意愿

1. **短剧承制方/工作室**（核心客群）：单部剧预算 5-50 万元，对「降低试错成本、加快交付」付费意愿极强；痛点是一致性与批量产能。客单价可按「剧」收：如 999 元/部（含 N 积分包）。
2. **MCN 与平台方**：需要批量化、模板化生产，倾向团队席位制 + API 调用。
3. **独立创作者/写手**：价格敏感，用订阅制低门槛入门（如 99 元/月），靠积分充值变现长尾。
4. **电商内容团队**（电商设计室客群）：按 SKU 套图包收费。

## 3.2 收费模型设计（积分体系为基座）

- **订阅分层**：免费体验（送积分 + mock 演示）→ 创作者版（月订阅 + 月赠积分）→ 工作室版（多席位 + 团队协作 + 优先队列）→ 旗舰/API 版（开放平台）。
- **积分定价锚点**：按供应商成本的 3-5 倍定价积分。例如一张 16:9 资产卡线上成本约 0.2-0.5 元 → 定 14-16 积分（1 积分 ≈ 0.03-0.1 元按充值档浮动）；本地 ComfyUI 生成按「电费 + 折旧」定极低积分，作为订阅增值（「自有 GPU 抵扣」），既保护毛利又奖励活跃。
- **成本护栏**：live 模式扣费已打通；补齐 P2-1 成本看板后可做「单剧成本熔断」（超预算需确认），避免失控。

## 3.3 收入结构（三曲线）

1. **第一曲线（现在）**：订阅 + 积分充值。关键是把免费到付费的转化点放在「真金白银的出图/出视频」环节（mock 演示免费，live 出图扣积分），并在出片前检查/批量生成等高价值动作处做付费引导。
2. **第二曲线（3-6 个月）**：按剧/按集的项目制收费（承制方全流程包干）与 API 开放平台（调用分成）。字幕/多语言出海作为加购包。
3. **第三曲线（6-12 个月）**：模板与资产市场抽成（10-20%）、训练服务（角色 LoRA 训练按次收费）、白标部署（给 MCN/平台私有化，年license）。

## 3.4 获客与增长

- **内容自证**：用万幕生制作 demonstrational 短剧发布到抖音/快手/视频号，片尾标注「由万幕生 AI 制作」——产品自产内容即获客素材。
- **创作者社区**：剧本模板/资产卡/工作流分享社区，UGC 沉淀抬高迁移成本。
- **渠道合作**：与短剧承制公司、网文平台（IP 改编上游）合作，提供「小说 → 分集 → 成片」管道。
- **成本优势传播**：本地 ComfyUI 模式 = 电费价出图，对预算敏感的工作室是决定性卖点。

## 3.5 合规与风险控制

- AIGC 内容标识与审核双端（P2-2），规避平台下架与政策风险；
- 供应商多元化（已具备），避免单一上游涨价/断供；
- 用户生成内容版权条款（平台仅提供工具、素材责任归用户）；
- API Key 加密存储（当前明文落库，需尽快加信封加密）与审计日志。

## 3.6 90 天商业化节奏建议

- 第 1-30 天：P0 三项（云端视频、任务队列、合成导出）+ 充值/订阅支付打通 + Key 加密；
- 第 31-60 天：封闭内测 5-10 家工作室（免费换反馈与案例），上线成本看板与字幕烧录；
- 第 61-90 天：公开定价上线，内容渠道投放（自产短剧案例），启动 API 内测与资产库。

---

# 第四部分：风险与依赖清单

| 风险 | 影响 | 缓解 |
|---|---|---|
| 供应商价格/政策波动 | 成本与可用性 | 多供应商 + 本地 ComfyUI 双通道（已具备） |
| 长任务稳定性 | 用户体验 | P0-2 任务队列 + 断点续跑 |
| 内容合规 | 下架/处罚 | P2-2 双端审核 + 标识 |
| 单点数据库 | 可用性 | 定期备份 + 托管 PG 迁移计划 |
| 前端大列表性能 | 体验 | 已有渐进渲染 + 虚拟化储备 |

---

# 结语

万幕生已完成「从想法到成片」的流程闭环，并把「模型无关、成本可控、一致性优先」三个工程决策做进了架构。下一阶段的主线非常清晰：**把视频/音频的真实产能补齐（P0），把长任务与协作做稳（P1），再用积分与订阅把价值变现（商业化）**。按本计划推进，90 天内具备商业化内测条件，6-12 个月内可形成「工具订阅 + 按量计费 + 生态抽成」的三曲线收入结构。

---

# 第五部分：现有功能深度盘点（逐模块能力清单与缺口）

> 本部分把第一部分的模块分析落到「能力点」粒度，作为后续开发的需求底账。每条标注状态：✅ 已完成 · 🟡 部分完成 · ⬜ 未开始。

## 5.1 工作台（Workbench）

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 四类媒体生成（文本/图片/视频/音频） | ✅ | live 下音频需接 TTS 模板 |
| 参考素材上传与 @ 引用 | ✅ | 引用素材应可跨模块复用（进资产库） |
| 模型/参数选择（动态模型数据） | ✅ | 缺参数 presets 记忆（按用户保存上次选择） |
| 积分扣减与余额显示 | ✅ | 缺积分流水账单（P2-1） |
| 作品画廊（横向滚动/hover 播放） | ✅ | 缺筛选/删除/下载/送入工厂 |
| 生成历史持久化 | ⬜ | 建 GenerationRecord 表，支持回溯与重放 |
| 提示词模板库 | ⬜ | P2-4 生态项 |

## 5.2 画布（Canvas）

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 七类节点 + 连线依赖 | ✅ | 节点产物 → 资产库互通 ⬜ |
| 沉浸式操作页（工具条居中/抽屉/磁性连线） | ✅ | — |
| 导演台集成（iframe + postMessage 投递） | ✅ | 导演台版本升级联调 |
| ⌘S 保存 / 撤销重做 | ✅ | 服务端历史版本 ⬜ |
| 多人协同编辑 | ⬜ | P1-4（Yjs） |
| 工程导出（JSON/图片包） | ⬜ | P2 |

## 5.3 影视工厂——建档与会诊

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| INTAKE 粘贴剧本 + AI 立项 | ✅（已改弹窗） | 支持 docx/fountain 文件导入 ⬜ |
| 分析审阅（推荐集数/时长/风格微调） | ✅ | — |
| finalize 生成分集 | ✅ | 分集粒度策略（按标记/按 AI 推荐） |
| 会诊五类诊断 + 勾选改写 + 对话 | ✅ | 会诊结果跨版本对比 ⬜ |
| 台词优化 | ✅ | 差异对比视图（diff 高亮）⬜ |
| 补缺漏提取（merge + 捕捉关键词 + 图片模型适配） | ✅ | — |
| 妆造兜底（默认造型） | ✅（本次） | 造型派生链（造型 → 妆造细节）可视化 ⬜ |

## 5.4 影视工厂——资产管理

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 角色设定卡参考图（卡面构图模板） | ✅（D1） | 真实出图效果需 ComfyUI/线上模型回归测试 |
| 道具设定卡 / 场景设定卡 | ✅（D1） | 同上 |
| 四类提示词模板独立配置 | ✅ | 模板市场 ⬜（P2-4） |
| 资产卡操作条（大图/下载/上传替换/编辑/锁定/合并/删除） | ✅ | 批量选择操作 ⬜ |
| 空间资产（多角度/侧别锁定/光影设计卡） | ✅ | 调度图与人群调度卡的持久化落库 ⬜ |
| 妆造库（默认造型兜底 + 新建造型） | ✅ | 造型 → LoRA 训练（P1-2） |
| 资产跨剧本复用 | ⬜ | P1-3 资产库 |

## 5.5 影视工厂——分镜系统

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| AI 拆分镜（mock + live） | ✅ | 拆分粒度参数（时长上限/镜数上限）UI ⬜ |
| 镜组分段（segmentTitle + 分组渲染） | ✅（本次） | 段名可编辑/拖拽重组（P1-1） |
| 出片前检查（必须/建议/衔接三类） | ✅（本次重排） | 一键修复计划（串联动作）⬜ |
| 段资产弹窗（补全所有/重新生成/逐张出/安全改写） | ✅（本次） | 调度图/人群调度卡持久化 ⬜ |
| 编辑整段引用（场景/人物造型/道具） | ✅（本次） | 引用可视化（卡面显示引用资产缩略）⬜ |
| 分镜提示词时间轴 | ✅（本次） | 点击 chip 定位到镜头卡 ⬜ |
| 逐镜出图/出视频 + 批量生成视频 | ✅ | 服务端队列（P0-2） |
| 视频占位框按画幅动态 | ✅（本次） | — |

## 5.6 影视工厂——视频与后期

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 逐镜视频生成 | ✅ | 云端供应商接入（P0-1） |
| 批量出片队列 | ✅（前端队列） | 服务端 Job（P0-2） |
| BGM / 配音 | ✅（mock） | live TTS 模板 ⬜ |
| 后期合成导出 | 🟡（面板在，逻辑 mock） | P0-3 ffmpeg 合成 |
| 字幕生成 / 烧录 | ⬜ | P0-4 |

## 5.7 剧本创作

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 弹窗新建 + 编剧工作区 + 版本历史 | ✅ | 协作编辑（P1-4） |
| 草稿恢复（前进后退） | ✅ | — |
| 导入影视工厂 | ✅ | 反向同步（工厂改动回写编剧线）⬜ |
| 结构化重写（按爆款结构/会诊建议重排） | ⬜ | P2-4 |

## 5.8 平台基座

| 能力点 | 状态 | 缺口与后续 |
|---|---|---|
| 认证 / 工作区 / 成员 | ✅ | 角色权限（P1-4） |
| 积分扣费 / 演示重置 | ✅ | 流水账单 / 支付 ⬜ |
| 媒体库（上传/访问） | ✅ | 容量配额 / CDN ⬜ |
| MOCK 开关（运行时） | ✅ | 仅管理员可切（P1-4 权限） ⬜ |
| API Key 加密存储 | ⬜ | 高优先级安全项 |
| 深色主题 / 字号 7 级 / 弹窗 4 档设计规范 | ✅ | — |
| Docker / 冒烟 / E2E | ✅ | CI（GitHub Actions）⬜ |

---

# 第六部分：重点工程的详细技术方案

## 6.1 P0-2 服务端任务队列（Job）详细设计

**数据模型**：

```prisma
model Job {
  id          String   @id @default(cuid())
  workspaceId String
  scriptId    String?
  type        String   // split | asset_image | storyboard_image | storyboard_video | subtitle | export
  payload     Json
  status      String   @default("queued") // queued|running|done|failed|canceled
  progress    Int      @default(0)
  step        String?  // 当前步骤文案
  result      Json?
  error       String?
  createdAt   DateTime @default(now())
  finishedAt  DateTime?
  @@index([workspaceId, status])
}
```

**Worker**：`scripts/worker.mjs` 常驻进程（或 dev 下随 server 启动的进程内循环），领取 queued 任务（SELECT ... FOR UPDATE SKIP LOCKED 语义用事务实现），按 type 分发到与 route 相同的服务函数（把 route 内逻辑抽到 `src/services/pipeline/`）。

**进度推送**：SSE 端点 `/api/jobs/stream?ids=...`，worker 每次更新进度写 DB，SSE 轮询 DB（500ms）推送；前端任务中心订阅。

**取消**：status 置 canceled；执行中的步骤在每步开始前检查 canceled 即可（粒度 = 逐镜）。

**迁移既有逻辑**：`assets/generate` 批量、`storyboards` split（image/video 模式）、批量视频循环全部改为投递 Job；同步 route 保留（单镜即时生成场景）。

## 6.2 P0-3 合成导出（ffmpeg）详细设计

- 服务端方案：`fluent-ffmpeg` + 静态 ffmpeg 二进制（docker 镜像内置）；输入 = 按分镜顺序的 videoUrl 列表 + SRT + audioUrl。
- 步骤：① 逐镜标准化（scale/crop 到 targetAspect、fps 30、静音轨填充）② concat ③ 字幕烧录（subtitles filter，样式：字号/描边/底边距）④ 音轨混音（BGM 音量 -12dB 侧链）⑤ faststart MP4 输出。
- 浏览器方案（备选/轻量）：ffmpeg.wasm 单线程较慢，仅用于 <60s 预览版。
- 产物：MediaFile + Export 记录；下载链接带有效期。

## 6.3 P1-2 角色 LoRA 训练详细设计

- 触发：角色详情页「训练专属模型」→ 上传/选择 10-20 张图（可从已出图资产与生成图挑选）→ 选择基底（Z-Image/Wan/SDXL）→ 提交训练 Job。
- 本地训练：ComfyUI 侧部署训练插件（用户 Win11 4090/7900XTX 均可），平台通过 ComfyUI API 提交；云端训练：接 Replicate/fal.ai。
- 产物：lora_name 登记 → 出图时资产生成路由在 Z-Image 图中自动注入 LoraLoaderModelOnly 节点（模板已具备该节点类型）。
- 计费：训练按次高价积分；使用 LoRA 出图按普通价。

## 6.4 P1-4 协作与权限详细设计

- 权限：WorkspaceMember 加 role（owner/admin/editor/viewer）；session 扩展 role；requireXxx 校验函数按 role 分层（viewer 仅 GET）。
- 编辑锁：Storyboard/Script 加 lockedBy/lockedAt；进入编辑置锁，30s 心跳续期，释放解锁；他人进入显示「X 正在编辑」。
- 审计：AuditLog(workspaceId, userId, action, target, meta)，关键写操作埋点。
- 实时：剧本正文先做「段级锁 + 轮询刷新」（成本低），Yjs 二期。

## 6.5 统一模型体系的演进清单

1. CustomModel.apiKey 信封加密（AES-GCM，主密钥环境变量）。
2. constraints 增加计费曲线（按 resolution/duration 阶梯）。
3. 工作流模板库：ComfyUI 工作流 JSON 上传（存储到 MediaFile 或新表），constraints.workflow 引用；配套「参数探针」（自动扫描 LoadImage/文本输入节点供用户映射）。
4. 模型健康度：后台定时 ping，连续失败自动摘除并告警。
5. 模型分组与排序（设计文档 O5 遗留项）。

---

# 第七部分：竞争环境与差异化打法

## 7.1 竞品格局

- **全能型**：剪映（AI 成片）、即梦、Runway、Pika——强生成、弱流程，短剧生产的专业流程管理不是它们的重心。
- **垂直新秀**：部分 AI 漫剧 SaaS 聚焦「小说转漫剧」，但普遍缺少资产一致性体系与本地 GPU 支持。
- **自建团队**：头部承制方自研流水线，成本高、迭代慢——万幕生可输出白标。

## 7.2 差异化三支柱

1. **一致性资产体系**（角色设定卡 + 妆造 + 空间资产 + 引用 + 锁定）是同类产品中最完整的；
2. **双通道产能**（线上 API 为主 + 本地 GPU 辅助）给用户「云上省心、本地省钱」的选择权；
3. **流程状态化**（进度规则、出片前检查、镜组管理）让多集长剧的项目管理成为可能，而不是一次性的 prompt 玩具。

## 7.3 定价示例（上线建议价）

| 档位 | 价格 | 权益 |
|---|---|---|
| 免费体验 | 0 | 200 积分 + mock 演示 + 单工作区 |
| 创作者 | ¥99/月 | 2000 积分/月 + 全流程 + 字幕 |
| 工作室 | ¥499/月 | 12000 积分/月 + 5 席位 + 任务队列 + 优先支持 |
| 旗舰/API | ¥1999/月起 | 60000 积分/月 + API + 白标选项 |
| 积分包 | ¥30 起 | 按量充值，永不过期 |

（积分内含毛利率目标 ≥65%；本地 ComfyUI 模式积分 5 折以鼓励自有 GPU。）

---

# 第八部分：组织与执行节奏

- **角色配置建议**：全栈 ×2、算法/工作流 ×1、产品 ×1（兼测试）、运营 ×1。
- **迭代节奏**：双周一迭代；每迭代固定 20% 工时还技术债（当前技术债：API Key 加密、任务队列、standalone 构建 ENOENT、E2E 环境抖动治理）。
- **里程碑**：
  - M1（+2 周）：P0-1/P0-2 上线，视频产能稳定；
  - M2（+4 周）：P0-3/P0-4 合成导出 + 字幕，封闭内测；
  - M3（+8 周）：P1-1/P1-3/P1-4，公开定价；
  - M4（+12 周）：API 开放平台内测 + 资产库/市场雏形。

---

# 第九部分：验收与质量红线（工程约定重申）

1. 每次提交前：`npx tsc --noEmit` 0 错误、`npm run build` 成功、smoke 17/17、E2E 全绿（mock 模式）。
2. 新增路由必须重启 dev（路由清单缓存），文档已记录。
3. 任何「假进度」不得回潮：一切进度必须来自真实事件（逐项完成度、里程碑或服务端上报）。
4. 深色主题 + 7 级字号 + 4 档弹窗规范不可破坏（globals.css 与 AGENTS.md 4.5）。
5. live 模式禁止静默回退 Mock：不可用必须给用户明确原因与修复指引。
6. API Key 等敏感信息永不入库为明文（待加密方案落地前的过渡期已在文档中标注）。

---

# 结语

万幕生当前已经站在「能跑通」与「能卖钱」的分界线上：流程闭环、模型无关、资产一致性体系与质量基线都已就位。接下来 90 天的每一项投入（视频产能、任务队列、合成导出、协作与计费）都直接服务于商业化内测。建议严格按照本文件第二部分的优先级推进，避免在生态类功能（模板市场、API 平台）上提前分散火力——先把 100 个付费的工作室用户服务好，再放大。

---

# 第十部分：《API调试计划书》15 项需求交付对照表

> 本轮已全部落地（commit 4455acb），逐条交付状态与后续深化方向：

| # | 需求 | 交付 | 深化方向 |
|---|---|---|---|
| 1 | 全局 loading 真实性 | 移除资产批量生成的假百分比；拆分镜改居中不确定态；其余为里程碑/逐项真实进度 | Job 队列后接入服务端真实进度 |
| 2 | 新建剧本改弹窗 | 列表页 Dialog 承载 IntakeForm（embedded 模式） | 表单分步向导（粘贴→配置→确认） |
| 3 | 出资产拆两步 | 立项弹窗只提取描述词；右栏「重新出图」一次性全出 | 「重新出图」支持只重出未出图项（当前全量） |
| 4 | 资产图放大镜 | 三类卡悬停「查看大图」全屏预览 | 预览中加缩放/平移与下载 |
| 5 | 资产卡面构图 | 三类模板按原型分区输出（image1/2/3） | 真实出图回归后微调措辞；A/B 两版模板 |
| 6 | 描述词适配图片模型 | extract 携带 imageModel 注入 [目标图片模型] 说明 | 记录映射关系到资产（溯源） |
| 7 | 妆造库空数据 | 提取事务后默认造型兜底 | 补缺漏时的造型智能命名 |
| 8 | live 未找到模型 | kind 回退 + 文案指引 /ai-settings | 前端选择器 live 下只出可用模型（已做） |
| 9 | 进度规则 | WorkflowTabs assetsReady 驱动 | 分集级进度（每集独立 chips） |
| 10 | 拆分镜交互 | 居中「AI 正在拆分镜…」+ 停止 chip + 时间轴 + 整剧完成 toast | 真取消（Job 取消）与分段进度 |
| 11 | 拆分完成页 | 镜组分段卡 + 出片前检查（三类）+ 批量生成 | 检查项可配置规则 |
| 12 | 段资产弹窗 | 补全所有/重新生成/逐张出/安全改写/首帧图/调度图/人群调度卡 | 调度图/人群卡持久化到段 |
| 13 | 编辑整段引用 | 场景/人物造型/道具默认匹配 + 按条应用 refs | 引用在卡面可视化缩略 |
| 14 | 视频占位 | 占位框按画幅动态 + 生成视频/批量生成视频 | 队列化与断点续跑（P0-2） |
| 15 | 遵循新 API 规范 | 全部经 AIService/CustomModel 体系 | — |

# 第十一部分：性能与数据治理规划

## 11.1 性能预算

- 首屏（工作台）LCP ≤ 2.5s（4G 网络）；分镜区 100+ 镜滚动不掉帧（已用渐进渲染，待虚拟化）。
- 数据库：storyboard 单集上限 120 条（当前唯一索引 episode+number），查询全走索引；MediaFile 字节改对象存储（S3/OSS）后 DB 仅存地址——当前 bytes 落库方案在媒体数量大后必须迁移，这是**数据治理第一优先级**。
- 图片/视频产物：迁移到对象存储（或 ComfyUI 主机 nginx 静态目录），MediaFile 保留元数据。

## 11.2 备份与恢复

- 内嵌 PG 每日 `pg_dump` 到备份目录并轮转 7 天；迁移到托管 PG 后启用 PITR。
- 恢复演练每季度一次（已具备全新环境演练脚本基础）。

## 11.3 可观测性

- 结构化日志（现有 console + [api] 前缀规范化为 JSON）；
- 上游 AI 调用埋点：供应商、模型、耗时、成败、token/积分——为成本看板与 SLA 提供数据；
- 前端错误上报（Sentry 或自建）。

---

# 第十二部分：关键决策答疑（FAQ）

**Q1：为什么 live 模式要「明确报错」而不是回退 Mock？**
真实费用与真实失败都要让用户看见。静默回退会让用户把 Mock 当成真结果交付，属于产品级事故。已修的模型回退（按 kind 找启用模型）是「真实通道内部的降级」，两者性质不同。

**Q2：ComfyUI 关机期间 live 出图/出视频怎么办？**
当前会明确报「ComfyUI 不可达」。要全时可用，二选一：① 在 /ai-settings 用模板接入一个线上出图 CustomModel（wan2.7-image 等）；② 保持 Win11 常开。架构上两者可并存，优先级可在模型排序里控制。

**Q3：镜组（segment）是硬规则还是 AI 自由发挥？**
AI 拆分时按「每 5-7 镜一组、组名 = B0x·情节拍·类型」输出；旧数据/异常数据前端按每 6 镜自动分组兜底。段名与分组支持后续编辑（P1-1 拖拽重组）。

**Q4：积分为什么在 live 下照扣？**
防失控 + 毛利保护。本地 ComfyUI 成本极低，后续可通过「自有 GPU 折扣」让利，而不是免费。

**Q5：为什么把字幕/ASR 做进模型体系而不是写死 Whisper？**
字幕只是能力之一，供应商会换（Whisper/FunASR/云厂商）；模型体系让它与其他能力同权管理、计费与切换。

**Q6：多集长剧的「整剧拆分镜」与现在的单集拆分是什么关系？**
当前按集拆分（每集一次请求），toast 文案先行对齐原型。P0-2 队列落地后，「整剧拆分镜」= 投递 N 个集级 Job 的编排任务，进度在任务中心聚合展示。

---

# 第十三部分：附录

## 13.1 关键文件索引

| 领域 | 文件 |
|---|---|
| AI 抽象层 | `src/services/ai/index.ts`（mode-aware 工厂）、`types.ts`、`live-ai.service.ts`、`mock-ai.service.ts`、`live/openai-chat.ts` |
| ComfyUI | `src/services/ai/comfy/client.ts`、`workflows/minimax-h3-r2v.api.json` |
| 统一模型 | `src/lib/plugins/{templates,invoke}.ts`、`/api/plugins/*`、`/api/ai/models` |
| 设置 | `src/lib/settings.ts`、`/api/settings/ai`、`AiServiceSettings.tsx` |
| 影视工厂 | `src/components/creation/film-factory/**`、`/api/scripts/**`、`/api/storyboards/**` |
| 资产提示词 | `src/lib/asset-prompt.ts` |
| 剧本创作 | `src/components/creation/script-writing/**`、`/api/writing-projects/**` |
| 计划与日志 | `docs/plans/*.md`、`docs/superpowers/specs/*.md` |

## 13.2 供应商与凭据现状

| 供应商 | 用途 | 状态 |
|---|---|---|
| 阿里云 token-plan（OpenAI 兼容） | 文本主力（qwen3.7-plus）+ 潜在出图（wan2.7-image 待查接口形态） | 已配置并验证 |
| DeepSeek | 文本辅助 | 已配置并验证 |
| ComfyUI（192.168.1.12） | 图片/视频本地通道 | 地址已登记，主机当前关机，恢复后设置页测试连接即可 |
| 火山 Seedance / 可灵 / Whisper | 视频与 ASR 模板 | 模板就绪，待用户填 Key |

## 13.3 交付节奏建议（重申）

M1 视频产能与队列（2 周）→ M2 合成导出与字幕（4 周）→ M3 协作/资产库/公开定价（8 周）→ M4 API 平台与生态（12 周）。每里程碑结束对照本文件第五部分的能力底账做一次覆盖度复盘。

---

*本文档由 AI Agent 于 2026-09-11 基于仓库全量代码、历次执行计划/日志与产品原型整理，供决策与排期使用。如有与代码不一致处，以仓库当前 HEAD（含 docs/ 底账）为准。*
