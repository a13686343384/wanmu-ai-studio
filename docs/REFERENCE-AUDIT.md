# 产品需求参考图对照与补漏记录

日期：2026-09-09。依据：`产品需求参考图片/` 下全部 98 张图（剧本创作19、画布操作页26、影视工厂40、首页/列表/导航13）。本次按图浏览并结合源码核对；浏览器重点验证画布和新增剧本创作闭环。参考图的真实生成内容不代表当前 Mock 的输出质量。

## 画布操作页

| 参考图 | 核对结果与本次处理 |
| --- | --- |
| 1 整体、2 文本、3 图片、4 视频、5–7 音频 | 七类节点已有。修复连接点偏离边缘、指针跟随抖动；真实端点与可见加号分离，固定坐标。输入区禁止拖动，节点高度变化时重新计算端点。Composer 的模型、画幅、分辨率、时长和歌词模式改为可操作并持久化。 |
| 8–15 导演台 | 已嵌入静态3D编辑器。本轮保留其功能，增加 iframe 消息来源校验；未逐一验收静态包内的骨骼/姿势/机位功能。 |
| 16 导演台截图 | 现有截图写入节点并建立连线，连线现在贴住卡片边缘。截图为 data URL 随画布保存。 |
| 17 资产 | 原资产目录为空且按钮无行为。新增当前工作区上传素材列表、搜索、上传和引用；选中同类节点时填入，未选中时新增节点。跨项目同工作区可共享上传文件。素材分类/收藏、生成产物自动入库尚未实现。 |
| 18 小地图、19 智能对齐、20 网格、21 适应画布、22 工程截图、23 缩放 | 小地图外框176×96而SVG200×150，造成裁切；现统一176×100。地图与底部工具栏分区，窄屏换行。补地图显隐、智能位置对齐（Shift临时关闭）、PNG工程截图。缩放显示改为订阅实时视口。缩放预设弹窗未做，已有加减和适应画布。 |
| 24 侧栏节点定位 | 节点列表随增删同步；定位改为依据节点真实尺寸适配视口，选中对应节点。收起后可重新展开。 |
| 25 Agent | 原发送只清空输入。现在通过AI抽象层生成文本/图片/视频/音频，把产物写入画布；选中的文本参与上下文。对话记录仅当前侧栏会话，生成节点持久化。多步骤自动执行工作流尚未实现。 |
| 27 项目菜单 | 保留已有项目导航/创建/删除。节点新增复制快捷键⌘D；撤销记录调整到拖动开始前，删除节点/连线前留档。 |
| 用户反馈的稳定性 | 去掉磁吸把手的DOM位移与CSS transform动画互相反馈。拒绝自连和重复连线。加载失败提供重试并禁止保存空图。保存请求串行处理，失败保留未保存状态。右键上传独立保存落点，不再依赖已关闭菜单或旧节点状态。 |

上传素材新增 MediaFile 表，按工作区成员鉴权读取，单文件上限20MB。当前适用于本地部署；大文件对象存储、配额、素材删除回收尚需后续扩展。

## 新增剧本创作

| 参考图（按时间顺序） | 本次落地 |
| --- | --- |
| 150251、153018 列表 | 独立剧本列表、搜索、创作状态统计、卡片、新建和删除。与影视工厂列表分离。 |
| 150329、150345 新建 | 题材配方、剧名、故事核心、目标集数、单集时长；快捷预设与自定义输入；创建后进入工作区。 |
| 150857、150927 工作区/大纲空态 | 全屏编辑区、顶部功能入口、右侧编剧Agent、明确空态。 |
| 151027 蓝图模型弹窗 | 模型与积分展示，通过writeScript服务生成蓝图、人物档案、分集结构。 |
| 151109、151118、151745 Agent | 生成反馈、调整要求输入、模型选择；对话保存到项目文档。Mock记录调整方向并传递到后续生成上下文，不宣称真实语义推理。 |
| 151148、151214 角色档案 | 统一角色列表，生成正文时携带人物设定。独立角色编辑与关系图暂未提供。 |
| 151817 大纲 | 大纲查看、下载；Agent调整保留既有正文。 |
| 151841 版本历史 | 生成/正文编辑/恢复前保存完整内容快照，保留最近20版；可恢复并再恢复回去。 |
| 151906、151921、151939 正文生成 | 分集切换、模型弹窗、进度、正文空态/生成态/编辑、保存和复制。 |
| 152034长图、生成完成 | 正文可编辑与下载，全剧下载；导入已写正文到影视工厂，建立Episode/Character记录。重复导入返回原剧本，避免重复建档。导入后的两个项目独立编辑，不自动同步。 |

数据：新增 WritingProject（工作区归属、配置、JSON文档、revision）。修改使用乐观版本检查，事务内完成版本更新与余额条件扣减；过期版本返回409。正文有未保存提示，阻止切集覆盖。AI仍为Mock，通过独立接口便于之后接真实供应商。

## 影视工厂

| 参考图 | 已有能力 / 差距 / 本次处理 |
| --- | --- |
| 1–2 首页、建档 | 已有列表、统计、搜索、建档双栏表单与配置。 |
| 3–7 分析和审阅 | 已有加载、分析结果审阅、分集创建。取消请求、真正后台执行、任务恢复仍缺；页面进度部分为估算。 |
| 8–10 列表分组、详情 | 已有列表分组、状态标签、全屏详情、分集胶片条、正文与分镜分栏。 |
| 11 会诊 | 已有诊断、建议勾选、追问接口与应用。 |
| 12–13 AI复述 | 已有复述结果和镜组建议。 |
| 14–17 资产模型选择与生成 | 原“下一步”只弹提示。本次新增文本/生图模型、画幅、分辨率弹窗；只提取缺少类别，只为未出图资产生成，轮询刷新侧栏并显示真实完成数量。 |
| 18–20 人物/妆造/道具 | 角色/场景/道具已有提取和出图；妆造模型已有但独立妆造库生成流程未实现。 |
| 21 下一步拆分镜 | 本次依据资产出图情况推进按钮和动作，避免资产已生成仍停留在原提示。 |
| 22–29 拆分镜四Tab与进度 | 已有出图/纯拆分/视频/BGM配置与请求；后台队列、跨页面续跑和整剧长任务取消仍待实现。 |
| 30–32 镜组与出片检查 | 已有镜头卡片。本次新增基于描述、时长、失败状态的检查，缺首帧图提示，以及相邻景别/运镜变化的人工衔接核对；这不是视觉理解或LLM连续性检查。 |
| 33 补首帧图 | 新增检查项直接打开现有出图配置入口；参考图的首尾帧双面板尚未完整实现。 |
| 34–38 显示模式/BGM/下载 | 已有产物筛选、音频生成和后期面板；参考图的全部工具按钮、镜组级素材打包下载尚不完整。 |
| 39–40 反向提示词与视频弹窗 | 已有分镜级反向提示词和单镜头视频生成弹窗；剧本级统一禁止项下发仍缺。 |

仍需单独建设的链路：真实视频合成与导出（现有后期面板仍模拟进度）、封面归档、影视工厂到画布的资源导入、真实AI供应商、稳定后台队列。未将这些标记为已完成。

## 首页、画布列表和全局导航

首页4图：媒体切换、模型、参数、引用素材、画廊均有实现。现有生成API把音频模式固定为智能歌词，本次改为传入用户选择；扣积分改为余额条件更新，避免并发超扣。首尾帧素材到真实供应商、批量图片全部回传/保存仍不完整。

画布列表8图：已有个人/团队、搜索筛选、创建、右键菜单、团队弹窗。团队审批和共享额度未完整落地；已有目录封面/项目封面规则仍需和真实资产管理联动。创作中心菜单1图：导航已有，本轮剧本创作入口正式替换占位页。

## 验证记录

新增回归覆盖：缩放端点稳定、卡片边缘连线、地图SVG尺寸与窄屏底栏、拖动撤销、加载失败零保存、右键上传重载、提示词与参数重载、输入不删除节点；剧本创建/蓝图/正文/保存/版本恢复/导入、过期版本拒绝和未登录鉴权；资产配置入口、分镜检查。

最终执行结果见 `docs/PROGRESS.md` 本轮记录。视觉截图与临时探针在忽略目录 `.screenshots/audit/`，不进入提交。
